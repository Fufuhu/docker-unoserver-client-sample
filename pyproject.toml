[project]
name = "docker-unoserver-client-sample"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "unoserver",
    "jinja2",
]
